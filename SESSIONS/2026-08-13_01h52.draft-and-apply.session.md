# Draft and apply — the sync API, reshaped around the decision

**Date**: 2026-08-13 01:52
**Base**: `main` @ `def5bad` (v0.2.0 + phases 3, 4, 6)
**Status**: planned, not written. Awaiting review.

**Supersedes**: nothing. **Displaces**: phase 5 of
[`2026-08-11_21h28.v2-implementation-plan.md`](./2026-08-11_21h28.v2-implementation-plan.md), which is
planned in [`2026-08-12_12h49`](./2026-08-12_12h49.phase-5-native-fast-path.session.md) and waits —
it is invisible to a consumer and blocks nothing.

---

## 0. What was asked for

> A draft of the sync, which can then be executed. The draft carries the possible conflicts, and when
> you ask to apply what the draft describes, you hand it the decisions for those conflicts too. The UI
> handles that.

## 1. Most of it is already built, and nothing can execute it

`syncDryRun(a, b)` scans both folders without writing, runs the pairing guard, computes the merged
tree, **actually attempts every text merge** ([sync.ts:397](../src/sync.ts#L397) — the merged bytes are
computed and then thrown away), plans the exact file-system actions for each side, predicts the
transfers, and returns the digest both folders would land on.

So the draft exists. What does not exist is any way to *run* it. The caller looks at the preview and
then calls `sync()`, which **recomputes the whole thing from scratch** and may reach a different
answer, because the folders moved in between. The only bridge is `approveMerge`, a hook that receives
the same preview *inside* `sync()` and can veto — which is not draft-then-apply, it is one sync with a
human wedged in the middle, unable to say anything except no.

And the decisions themselves have no shape. The one channel is `resolveText`, a callback the engine
invokes mid-pass, for text only. A binary conflict cannot be decided at all before the fact: the copy
is always parked, and the caller resolves it afterwards with `node.resolve()`.

## 2. The seam is already in the code

`sync()` has exactly one line where planning ends and writing begins — everything above it is pure
computation over two scans, everything below touches disk:

```ts
const previewState = await stateDigest(target);   // sync.ts:243  <- the seam
...
await a.apply(target, chain(...));                // sync.ts:276  first write
```

Above the seam, `sync()` and `syncDryRun()` are **the same ninety lines twice**, and the preview
payload handed to `approveMerge` is a third copy of the same object literal. Nothing enforces that the
three agree. A dry run that predicts a sync the engine will not perform is not a bug anyone would
notice until it mattered.

Splitting at the seam is therefore not only what the consumer asked for. It deletes a duplication that
is one refactor away from lying.

## 3. Shape

```ts
const draft = await syncDraft(a, b, options);     // nothing is written
draft.changed        // false when the two folders already agree
draft.conflicts      // what needs deciding, with both versions and why it did not merge
draft.actions        // literal writes/deletes/renames per side
draft.transferred    // how many files would move, each way
draft.state          // the digest both would land on

const result = await draft.apply({ decisions });  // this writes
```

`sync(a, b, options)` stays exactly as it is, and becomes sugar for
`(await syncDraft(a, b, options)).apply()` — same signature, same result, one implementation
underneath instead of two.

**The draft is an in-memory object, not a document.** It holds the merged bytes and both node handles,
so it cannot be serialised or sent anywhere. What crosses a boundary is `conflicts` and `actions`,
which are plain data, and what comes back is `decisions`, also plain data. Worth stating because the
name invites the opposite assumption.

## 4. Decisions are data, and a decision mints a version with two parents

```ts
type Decision = 'a' | 'b' | Uint8Array;
await draft.apply({ decisions: { [uuid]: decision } });
```

**`'a' | 'b'`, not `'mine' | 'theirs'`.** A draft is between two peers and belongs to neither;
`node.resolve()` keeps `'mine' | 'theirs'` because it is local to one node and there the words mean
something.

The part that is not obvious: **choosing a side cannot mean adopting that side's entry.** Say A won by
last-writer-wins and the user picks B. B's version carries an older `updated`, so a third peer that
meets this mesh tomorrow re-runs the same comparison, reaches the same conclusion as the engine did,
and puts A's version back. The decision would silently undo itself.

So a decision mints a **new version**: the chosen content, `updated` above both, and **two parents** —
`prev` the winner's hash, `prev2` the loser's. That is precisely what the automatic text merge already
does, for exactly this reason, and there is already a test pinning it (*"record both parents, so a
third peer does not re-raise them"*). A decision is a merge performed by a person, and it has to leave
the same trace as one performed by `diff3`.

Handing back bytes is the same operation with content the caller supplied, which is also what makes
"the user edited both versions together in a three-column view" expressible.

## 5. A partially decided draft is the normal case

The user resolves two of five conflicts and gets on with their day. Undecided conflicts fall through
to exactly today's behaviour: last-writer-wins, the loser parked as a copy, a pending decision left in
`vfs.json`. Nothing about `apply()` requires a complete decision set, and nothing warns about an
incomplete one — a sync that refuses to proceed until every conflict is settled would be worse than the
one we have.

## 6. Staleness: check what you are about to touch, not the folder

Between drafting and applying, a user can save a file. Writing a plan built on a picture that has
moved is how a sync tool eats an edit.

The check is **not** a second scan. A full re-scan on every apply would double the cost of every sync
in the mesh, and on Drive the scan is most of what a sync costs. What the apply verifies is the paths
its own actions name: `mtime` and `size` against what the draft recorded, which is O(actions) stats
instead of O(tree). A file that changed somewhere the draft was not going to touch does not invalidate
anything — the next pass will pick it up, which is what the next pass is for.

A mismatch throws `StaleDraftError` carrying the paths that moved, so a UI can re-draft and show the
user what changed under them rather than a spinner and a shrug.

`sync()` passes `{ verify: false }`, because it applies a draft it made three microseconds earlier and
there is nothing to have changed in between.

## 7. What goes, what stays

| | |
| --- | --- |
| `approveMerge` | **Removed.** The draft is the approval, and a hook whose only vocabulary is "no" cannot express what the consumer needs. |
| `resolveText` | **Kept**, and re-documented as the *headless* path: a rule that resolves without a person (always take the longer one, always take the server's). Interactive resolution is decisions now. |
| `node.resolve()` | **Kept.** Copies parked by a background sync — the phone syncing at 4am with nobody watching — still have to be resolvable after the fact. This is also why `reason` on the durable copy still matters, though less. |
| `syncDryRun()` | **Becomes `syncDraft()`.** A thin deprecated alias is possible but pointless: no third-party consumers, and the plan already treats v2 → v3 as a hard cut. |
| `syncMesh` / `syncUntilStable` | Unchanged. They drive `sync()` per edge, which still exists. |

## 8. What this is not

- **Not a transaction.** `apply()` writes content, then logs, then the two headers, in that order, and
  an interruption leaves the same recoverable state it leaves today. A draft does not make two folders
  atomic and nothing here pretends otherwise.
- **Not multi-peer.** A draft is one edge, like a sync. Deciding a conflict across three peers at once
  is a different problem and nobody has it yet.
- **Not a queue.** Drafts are not stored, not resumable, and do not survive a reload. Re-draft.

## 9. Tests

- A draft writes nothing: both folders byte-identical afterwards, including `vfs.json` and the log.
- `sync(a, b)` and `draft.apply()` reach the same state, the same conflicts, the same transfers — the
  test that keeps the sugar honest.
- A decision for `'b'` when A won: B's content lands on both sides, and the entry carries both hashes
  as parents. Then a third peer holding A's version syncs and **does not re-raise it** — the same
  property the auto-merge has, driven by the same fixture.
- A decision handing back bytes: that content lands, with both parents.
- An undecided conflict in a partly decided draft still parks its copy.
- Editing a file the draft was going to write, then applying: `StaleDraftError` naming that path, and
  **both folders untouched**.
- Editing a file the draft was *not* going to touch: the apply proceeds.
- A `PairingError` surfaces from `syncDraft()`, before anything is written — it already does, and the
  split must not lose it.

## 10. Documentation

`docs/api.md`: `syncDraft`/`apply`/`decisions` replacing the `syncDryRun` section, with `sync()`
described as the one-shot form rather than as the primary. `docs/conflicts.md`: the decision flow
becomes the main story and `node.resolve()` the after-the-fact one. `README.md` if the headline
example changes — it should, because "look, then decide, then apply" is the honest shape of this
library.

## 11. Open questions

1. **`draft.apply()` versus `VFSNode.apply()`.** Two different objects, two different meanings — the
   node's takes a target tree and content, the draft's takes decisions. Live with the collision, or
   name one of them something else?
2. **Does a decision need to travel as *why*?** A version minted by a person and one minted by `diff3`
   are indistinguishable in the log. Recording which is which is cheap, and it is the same question as
   `'block'` (§15 of the phase 4 session) one layer up. Deliberately left open until the flow exists.

## 12. Commit message

```
look before you sync, and decide before it writes
```

---

# Extension — 2026-08-13 02h10

**Summary**: replaces the shape of §3–§6. There is no draft object and no second call to execute one:
`sync()` plans, and **refuses to write anything when a conflict needs a person**. It reports, the UI
decides, and the decisions come back into another ordinary `sync()`. Adds the definition of "needs a
person" (§14), the consequence for peers with nobody watching (§15), and what this deletes from the
plan above (§17).

## 13. The shape, revised

```ts
const result = await sync(a, b);
if (result.pending.length > 0) {
  // nothing was written. result.pending is what the UI shows.
  const decisions = await ui.decide(result.pending);
  await sync(a, b, { decisions });
}

await sync(a, b, { dryRun: true });   // plans and reports, never writes
```

One function, one return type, one seam. `syncDryRun()` collapses into `sync(a, b, { dryRun: true })`
and stops existing — with it goes the ninety-line duplicate of §2 and the third copy of the preview
literal inside `approveMerge`.

`decisions` is an **array**, mirroring `pending` one for one, because that is the shape a form
produces and the shape that survives a round trip through a UI:

```ts
type Decision = { uuid: string; choice: 'a' | 'b' | Uint8Array };
```

§4 above still holds in full and is the load-bearing part of this session: a decision **mints a new
version with two parents**, or it undoes itself the next time a third peer does the arithmetic.

## 14. "Conflict" is too broad to abort on. The word is *pending*

Not everything the merge reports as a conflict needs a person, and aborting on all of them would stop
a sync for things the engine settled by itself:

| Reported | Needs a person? |
| --- | --- |
| a text conflict the three-way merge settled | **no** — it is resolved, and the report survives only so the caller can see it happened |
| `location` — the same file renamed differently on each side | **no** — one path wins, deterministically, and no content is at risk |
| `content` — two different versions of the same file | **yes** |
| `delete-edit` — one side deleted, the other edited | **yes**, and it is the sharpest of the three |
| `kind` — a file on one side, a folder on the other | **yes** |

So the result carries both: `conflicts` (everything that diverged, as today) and **`pending`** (the
subset a person has to settle). `sync()` aborts on `pending`, never on `conflicts`.

## 15. The consequence: a peer with nobody watching stops converging

This is the price of the rule and it has to be stated, not discovered.

Today a conflict never blocks a sync: the engine picks the newer version, parks the loser as a copy,
and **everything else keeps flowing**. Under the new rule, one disputed `gamelist.xml` between the
laptop and the NAS aborts that edge — and the 900 photos that have nothing to do with it do not
travel either, on every pass, until a human resolves the one file. The phone syncing at 4am has no
human, so it never resolves anything and never converges again.

The escape hatch is one option, and naming it is the whole point:

```ts
await sync(a, b, { onConflict: 'park' });   // today's behaviour: pick a winner, park the loser
```

Default `'abort'`. **The abort path is for an edge with a person on it; the park path is for an edge
without one.** A background sync passes `park` and keeps the mesh converging; the copies it leaves
behind are pending conflicts in `vfs.json`, which the UI resolves later through `node.resolve()` — the
mechanism that already exists and is exactly what it is for.

That also settles where `'block'` matters (§15 of the phase 4 session): on the parked path, which is
the one nobody was watching.

## 16. What atomic can and cannot mean here

What is being asked for is achievable and worth having: **the plan is complete before the first byte
is written, and no write happens at all when the plan needs a human.** Every conflict is known before
the seam, so the abort costs two scans and touches nothing.

What it cannot mean is a transaction. `apply` writes content, then log rows, then the two headers, in
that order, across two folders that may be on different machines. A network drop mid-write leaves the
partial state it leaves today, and the ordering is designed so the next pass heals it. No API shape
changes that, and this session does not claim to.

## 17. What this deletes from the session above

- **The draft object** (§3) — there is none. Nothing is held between calls.
- **`StaleDraftError` and verify-what-you-touch** (§6) — with no plan carried across calls there is
  nothing to go stale. The second `sync()` re-plans from scratch.
- **The `draft.apply()` naming collision** (§11.1) — moot.

One risk survives the simplification: a decision is made about a dispute, and the dispute changes
before the second `sync()` lands. The decision is keyed by `uuid`, so it would apply to a version the
user never saw. Cheap fix, and it stays optional: a decision may carry the two hashes it was made
about — the UI already received them in the report — and the engine ignores any decision whose hashes
no longer match, leaving that conflict pending and reporting it again.

## 18. Open question

`sync(a, b, options)` as a free function, or `a.sync(b, options)` as a method on the node? The request
was written as `.sync({ decisions })`, which reads like a method. The free function is symmetric —
neither peer owns the operation — and that symmetry is real: `sync` drives both sides equally. A
method would put one of them in charge in a way the engine does not.

## 19. Commit message, revised

```
a sync that will not write until somebody has decided
```

---

# Extension — 2026-08-13 02h24

**Summary**: settles the two questions of §15 and §18 and fixes the final surface. `conflictBehavior`
replaces the name `onConflict`; `sync` stays a free function.

## 20. The surface, settled

```ts
export interface SyncOptions {
  /**
   * What to do about a conflict a person has to settle.
   *
   * `'abort'` (default) writes nothing at all and reports them in `pending`.
   * `'park'` is the old behaviour — newer version wins, loser kept as a copy —
   * and it is what an edge with nobody on it wants, because an aborting sync
   * that nobody can unblock never converges again.
   */
  conflictBehavior?: 'abort' | 'park';
  /** Settles a reported conflict. Ignored when it names a dispute that no longer exists. */
  decisions?: Array<{ uuid: string; choice: 'a' | 'b' | Uint8Array }>;
  /** Plans and reports without writing either folder. */
  dryRun?: boolean;
  // ... conflictCopies, conflictName, now, heldAt, autoMerge, archives, resolveText, adopt
}
```

**`onConflict` was rejected as a name**: the `on` prefix reads as a callback, and everything else in
these options that starts that way *is* one. `conflictBehavior` says it is a policy.

It sits next to the option it is not: `conflictCopies` decides whether the loser is kept once a
conflict is being parked; `conflictBehavior` decides whether parking happens at all.
`{ conflictBehavior: 'park', conflictCopies: false }` is the existing "pick a winner and drop the
loser silently", unchanged.

**`sync(a, b, options)` stays a free function.** The symmetry is not cosmetic: `sync` drives both
sides, commits both, writes both headers in one pass. `a.sync(b)` would put one peer in charge of an
operation in which neither is.

## 21. Order of work

1. The seam: split planning from writing inside `sync()`, with `dryRun` returning at the seam. No
   behaviour change yet, and `syncDryRun` deleted in the same move — that is where the duplication
   dies.
2. `pending` as its own list (§14), which is a classification, not new machinery.
3. `conflictBehavior: 'abort'` as the default, and every existing test that expects a parked copy
   passes `'park'` — the diff of that change is the honest measure of how much behaviour this alters.
4. `decisions`, with the two-parent minting of §4 and the hash pinning of §17.
5. Documentation, and the README example, which becomes the new shape.

---

# Extension — 2026-08-13 02h41

**Summary**: `conflictBehavior` is dropped. `'abort'` is the only behaviour for now — validate it
first, add the escape hatch later if it turns out to be needed. Records what that leaves dormant and
why the decision is cheap to revisit.

## 22. No `park`, and therefore no option at all

The option of §20 does not ship. A conflict a person has to settle aborts the sync, and that is the
whole rule. Fewer things to explain, one path to specify for the second implementation, and one shape
to validate against a real consumer before deciding it was right.

**It is cheap to revisit**, which is what makes this the right way round: adding `conflictBehavior`
later, defaulting to `'abort'`, is additive — no caller changes, nothing breaks. Removing an option
that turned out to be a mistake is not. So the ordering is: ship the strict thing, learn, and widen
only if the mesh actually needs it.

## 23. What this leaves dormant, on purpose

Conflict copies are how a conflict has been *survived* until now: the loser parked beside the winner
so nothing is destroyed. With nothing parking, the machinery around them stops being reached from
`sync()`:

| | Status |
| --- | --- |
| `conflictCopy()` inside the merge | **still runs** — the merge describes the copy, and that description is most of what `pending` shows the user |
| copies actually written to disk | never, from a sync — the abort happens before the seam |
| `node.conflicts()` | answers empty, because nothing parks any more |
| `node.resolve()` | reachable, unused — it resolves copies that this version no longer creates |
| `conflictCopies` / `conflictName` options | inert until something parks again |

None of it is deleted. It is tested, it is correct, and it is exactly what a future
`decisions: [{ uuid, choice: 'later' }]` would need — the per-conflict version of `park`, decided by
the person instead of by a flag, which is a better shape than the option this extension is dropping.

## 24. The consequence, accepted rather than mitigated (superseded by §29)

An edge with nobody on it does not converge past a conflict. The phone syncing at 4am aborts, and
aborts again tomorrow, and the files that have nothing to do with the dispute wait with it. That is
known, it is the price of validating the strict rule first, and it is one option away from being
fixed on the day it hurts.

---

# Walkthrough — 2026-08-13 03h40

**Summary**: written and green. 508 tests pass (494 before: 14 new, 28 rewritten), `lint` and
`tsc --noEmit` clean. Steps 1–5 of §21 all landed. Three things went differently from the plan and
each has its own section: the escape hatch came back as a **decision** rather than an option (§29),
`approveMerge` survived (§30), and a dry run's `state` turns out to be a prediction with a caveat
(§31).

**Status**: implemented, awaiting review.

## 25. The seam, and the duplication it deleted

`sync()` is now `planSync()` + `applyPlan()`, split exactly where §2 said the line already was.
`syncDryRun()` is gone; `sync(a, b, { dryRun: true })` returns the plan at the seam. One result type
(`SyncResult`) replaces the two, so a preview and a sync can no longer describe different work.

`planSync` takes one parameter the two paths do not share: whether to persist the scan. A real pass
commits, a dry run scans and carries the rows a commit *would* have appended into the history, where
they answer the same questions. That is the only asymmetry left, and it is the reason `dryRun` is a
parameter instead of a second function.

## 26. `pending` is a classification, and `settled` is what makes it possible

`ConflictReport.settled` is set by the auto-merge when it produces content both sides adopt. Without
it, "a conflict occurred" and "a conflict is waiting for a person" are the same list, and the abort
rule would stop syncs over merges that already worked.

`needsDeciding()` is then two lines and the whole policy: not settled, and not `location`.

## 27. A decision mints a version with two parents

`applyDecisions()` runs after the auto-merge, over the same `merge` object, and mints the chosen
version exactly as `autoMergeText` mints a merged one: `updated` above both, `prev` the winner's
hash, `prev2` the loser's, a log row, and the parked copy removed.

The test that matters is *"mint a version with both parents, so nobody re-decides it later"*: a third
peer holding the losing version syncs afterwards and adopts the decision instead of re-raising it.
Without `prev2` that peer redoes the engine's arithmetic, reaches the engine's original answer, and
undoes the user's choice days later on a machine nobody is looking at.

Hash pinning landed as planned and has its own test: a decision naming a dispute that moved on is
ignored and reported again rather than applied to a version the user never saw.

## 28. What the abort rule actually cost, measured

Implementing it turned **28 tests red**, and none of them was a bug in the refactor. They were one
fact: nothing could park a conflict any more, so nothing could get past one.

| | |
| --- | --- |
| 13 | convergence — random histories no longer reach one state, because `syncUntilStable` stops at the first conflict |
| 2 | chains — "settles a conflict identically across the whole chain", "does not re-raise a settled conflict" |
| 13 | conflict copies, `node.resolve()`, held copies, cold archives |

The 13 convergence failures are the finding. It is not that an unattended peer stops converging
(§24); it is that **a mesh cannot converge through a conflict at all**, and the suite proving the
central property of the design cannot run.

## 29. So `park` came back — as a decision, not as an option

Reported, and the call was to keep `abort` as the only behaviour and rewrite the tests. That is what
happened, and rewriting them needed one addition: **`choice: 'both'`**, which keeps the winner and
parks the loser.

This is the per-conflict form §23 predicted, and it is better than the option this session dropped:
the answer comes from the person, per file, instead of from a flag that decides for every file in
advance. It is also the only path that now reaches the parking machinery, so nothing is dormant after
all — `node.conflicts()` and `node.resolve()` are reached again, by a user who said "keep both".

Two test helpers carry the new shape: `settle(a, b, choice)` — sync, answer whatever came back,
sync again — and `stabilise(edges)` for a mesh. The convergence suite now proves something stronger
than it did: the mesh converges **when decisions are supplied**, which is the workflow, rather than
when the engine decides silently.

## 30. `approveMerge` survived, against §7

§7 removed it because the draft was the approval. There is no draft any more, and without one
`approveMerge` is the only way to confirm-then-write in a **single pass**: the `dryRun` route costs
two, and the second one is another scan of both folders — which on Drive is most of what a sync
costs.

It is now fed the same `SyncResult` everything else uses, so the third copy of the preview literal
died with the duplication. It runs only when nothing is pending: there is nothing to approve about a
pass that is not going to write.

## 31. A dry run's `state` is a prediction with one caveat

`actions`, `transferred` and `pending` match the real pass exactly. `state` matches **only once both
sides have recorded their edits** — a version is stamped when it is recorded, and a dry run records
nothing, so the timestamps it invents for uncommitted edits are not the ones the real pass will
invent. Verified both ways and pinned by a test.

Not worth fixing: the fix is for a dry run to commit, which trades a documented caveat about a digest
nobody eyeballs for a "dry run" that writes.

## 32. Left out on purpose

- **The explorer** still confirms through `approveMerge` and does not show `pending`. It compiles and
  syncs; it just does not offer the new flow yet. That is app work, and it is the first honest test
  of whether this API is pleasant to consume.
- **`'block'` on the durable copy** (§15 of the phase 4 session) is still open, and this session
  moved where it matters: with a person in front of it, nothing is parked and `pending` carries the
  reason; the durable `reason` only matters for what `'both'` leaves behind.

## 33. Commit message

```
a sync that will not write until somebody has decided
```

---

# Extension — 2026-08-13 04h20

**Summary**: the catalogue asked for — every conflict the engine can raise, the data each one needs
before anyone can decide it, and the answers that are legal for it. Proposes one callback across both
levels (§35), and records what is missing from today's payloads (§38). Design only; no code.

## 34. Why one callback over two levels

A pairing refusal (`foreign-mesh`) and a file conflict (`content`) are different in every mechanical
way: the first is **one per pass** and settled before the merge is even computed, the second is **N
per pass** and settled after. Nothing forces them into one hook.

What does is the caller's side of it. Both are *"the engine stopped, and only a person can say what
happens next"*, both must be answered before anything is written, and both abort the pass when the
answer does not come. A consumer that has to learn two vocabularies for that has to learn two.

So: one callback, one discriminator, two cardinalities.

```ts
await sync(a, b, {
  decide: async ({ reason, ctxA, ctxB }) => {
    if (reason === 'foreign-mesh') return { action: 'adopt', wins: 'a' };
    if (reason === 'content') return { action: 'take', wins: 'b' };
    return null;            // no answer for this one — the pass writes nothing
  },
});
```

**Returning nothing aborts everything.** Not this conflict — the pass. That is the rule already in
place and the callback inherits it: all of them answered, or none of it happens.

**`wins: 'a' | 'b'`, not the context object.** The sketch passed `ctxA` back, which reads well and
works in one process, but a decision has to survive a trip through a UI — the same answers arrive as
`decisions` data today. A side is a string; an object is not.

## 35. The catalogue

### Level 1 — the two folders, before any file is looked at

One of these at most, per pass, and it is asked before the merge is computed: a `foreign-mesh` means
the merge must not happen at all.

| `reason` | What happened | Data needed to decide | Legal answers |
| --- | --- | --- | --- |
| `foreign-mesh` | Two different `syncId`s: separate meshes | both `syncId`s, live entry counts (*"1,240 files against 890"*), `peerId`s, log digests | `{ action: 'adopt', wins }` — both take that side's `syncId` · nothing → abort |
| `peer-collision` | One `peerId` on two folders: a copied `.vfs`, or a non-unique `options.id` | both sides' entry counts and log positions; whether either has ever synced | `{ action: 'reidentify', wins }` — the loser mints a fresh id · nothing → abort |
| `version-unreconcilable` | `vfs.json` formats that cannot be read as one | both `version` numbers, entry counts | `{ action: 'override', wins }` — that side's format is authoritative and the other migrates · nothing → abort |

`adopt: { syncId }` is today's answer to the first, as data. The second has a remedy that exists and
is unreachable from `sync()`: `node.reidentify()`. The third has none — `override` would mean writing
a migration, and until that exists the only honest answer is to abort.

### Level 2 — one file, after the merge and the auto-merges

| `reason` | What happened | Data needed to decide | Legal answers |
| --- | --- | --- | --- |
| `content` | Two versions, neither descended from the other | per side: `path`, `hash`, `size`, `updated`, `peerId`, and **whether the bytes can be read here**; plus `base` when an ancestor was found, and `textReason` when a merge was tried and refused | `take` (`wins`) · `both` · `content` (bytes the caller composed) |
| `delete-edit` | One side deleted, the other edited | which side is the deletion, when and by whom; the surviving version's `size`/`updated`; whether its bytes are here | `take` (`wins`) · `both` — resurrect *and* keep a copy · `content` |
| `kind` | A file on one side, a directory on the other, one path | both `kind`s, the path, and **how many entries live under the directory** — moving it aside moves its subtree | `take` (`wins`) — the loser is renamed aside, never deleted |
| `location` | The same file renamed differently on each side | both paths, who moved when | `take` (`wins`) — which path it keeps |

`location` is **not** raised by default and stays out of `pending`: one path wins deterministically
and no content is at risk. It is in the catalogue because a consumer may want to be asked anyway, and
because leaving it undocumented invites the question every time.

### Not a conflict, and worth stating

- The same edit on both sides — one hash, nothing to decide.
- One version descended from the other — propagation, not divergence.
- A rename only one side made — the mover wins, no report.
- A text conflict the three-way merge settled — reported as history, never `pending`.

## 36. `textReason` is context, not a conflict

`block`, `size`, `eol`, `no-base`, `unreadable` describe why an eligible text conflict was not settled
by `diff3`. They are fields **on** a `content` conflict, and each one changes what a UI should offer:

| | What the user should be offered |
| --- | --- |
| `block` | a three-column view — there is a base, and the edits are real |
| `eol` | *"the line endings differ; normalise them and this merges itself"* — the only one that never fixes itself |
| `size` | pick a side; a merge was never going to happen |
| `no-base` | pick a side, and say why: nobody kept the ancestor |
| `unreadable` | fetch the content first, then decide |

## 37. What today's payloads are missing

Most of the data above already travels. Three gaps, all small:

1. **"Can I read this version here?"** — a UI cannot offer a diff for bytes this peer does not hold.
   Derivable from `held` and `mtime` today, which means every consumer derives it independently.
2. **Subtree size on a `kind` conflict** — "this renames 412 files" is the whole decision, and it is
   not in the report.
3. **The pairing payload has no `decide` shape at all** — it arrives as a thrown `PairingError`.
   Turning a throw into a callback is the actual work of level 1.

## 38. Open

- The name. `approveMerge` described a hook that could only say no; this one decides. `decide` is the
  proposal.
- Whether `decide` and `decisions` both live, and they should: the callback is for a UI that answers
  in the moment (one pass), the array for a UI that goes away and comes back (two).

---

# Extension — 2026-08-13 05h10

**Summary**: the catalogue of §35 becomes a contract and moves to
[`docs/conflicts.yaml`](../docs/conflicts.yaml), audited against every site that raises a conflict.
The audit found one case missing (§39) and two payloads that were wrong about their own shape (§40).

## 39. `path-collision` was missing, and it was hiding inside `content`

[`resolvePathCollisions`](../src/merge.ts#L420) reports two **different files** claiming one path —
two uuids, two histories — with `kind: 'content'`, the same word `mergeEntries` uses for two versions
of **one** file.

They are not the same question:

| | two versions of one file | two files on one path |
| --- | --- | --- |
| uuids | one, shared | two |
| At risk | one version overwrites the other | nothing — the loser is renamed aside |
| The question | which version survives | which one keeps the name |

A consumer switching on `reason` gets `content` for both and has to notice that `a.uuid !== b.uuid`
to tell them apart. The contract names it `path-collision`, and keeps `kind` separate for the case
where a directory is involved — not for symmetry, but because a directory losing its name takes its
whole subtree along, and `subtree` is the number that decides it.

## 40. Two payloads that were wrong

- **`version-unreconcilable` is raised per side**, so it fires when *both* folders are behind — two
  v2 folders meeting a v3 engine. The earlier sketch assumed a choice between two formats, and there
  is often no other side to pick. The payload now carries `engine` and a per-side `readable`, which
  is what tells "could be read, no migration covers it" from "cannot be read at all".
- **`peer-collision` needed `everSynced`.** Choosing which folder reidentifies is otherwise blind,
  and the one that has never synced is the safe one.

## 41. What the contract deliberately refuses to offer

`version-unreconcilable` admits only `abort`. `migrate` and `override` would both mean rewriting a
folder into another format, and no such migration exists — an action in a contract is a promise, and
that one cannot be kept today.

## 42. Two sections that are not conflicts, and are in the file anyway

`not_conflicts` (what the engine settles alone) and `not_decisions` (what it throws, because no answer
would be safe: content arriving at the wrong hash, resolving with bytes held on another peer,
dematerialising the last copy). Both are there so the catalogue can be read as complete rather than
as a list someone stopped writing.

---

# Extension — 2026-08-13 05h40

**Summary**: settles how a conflict reaches the caller. Without a decision callback, `sync()`
**throws** with every conflict it found; with one, the callback is asked about them one at a time.
This absorbs `PairingError`, gives the two levels one vocabulary, and is planned here — not written.

## 43. Why a throw, when `pending` already worked

`result.pending` can be ignored by accident, and what it looks like when it is ignored is the
problem: a pass that stopped for a conflict returns `applied: false` and writes nothing, which is
indistinguishable at a glance from a pass that had nothing to do. A caller who never reads `pending`
has a sync that silently stops working the first time two devices disagree.

An exception cannot be ignored by accident. For the one library in the stack whose job is to not lose
files, that asymmetry decides it.

Two things in the code already lean this way:

- **Level 1 has always thrown.** `PairingError` obliges the caller to look; `pending` asks them
  nicely. Two grammars for "the engine stopped and only a person can say what happens next".
- **`syncMesh` already treats a throwing edge correctly** ([sync.ts:1218](../src/sync.ts#L1218)): the
  error goes in `MeshResult.error`, the other edges still run, and `syncUntilStable` does not count it
  as progress. An unanswered conflict *is* an edge that made no progress. Nothing there changes.

## 44. The shape

```ts
try {
  await sync(a, b);                    // no callback: it applies, or it throws
} catch (error) {
  if (error instanceof ConflictError) error.conflicts;   // every one, in the contract's payload
}

await sync(a, b, { decide: async (conflict) => answer });   // callback: one at a time
```

**One error, one list, one payload.** `ConflictError.conflicts` is an array of the payloads in
[`docs/conflicts.yaml`](../docs/conflicts.yaml), and a pairing refusal is simply an array of one with
`level: 'pairing'`. `PairingError` is absorbed: it is public API and this is a breaking change, but
keeping both is keeping the double grammar this exists to remove, and the plan already treats v2 → v3
as a hard cut with no third-party consumers.

## 45. Three edges, settled

- **`dryRun` never throws.** Its job is to report, and it has to stay additive — delete the flag and
  nothing else changes. It returns the conflicts in the result, as it does now.
- **Declining is not throwing.** A `decide` that answers `abort` returns normally with
  `applied: false`. The throw is for *"nobody could decide"*; a person cancelling a dialog answered
  the question, and a cancelled sync is an outcome, not a failure.
- **`decisions` and `decide` both live.** The array is for a UI that goes away and comes back, the
  callback for one that answers in the moment. A conflict answered by both takes the array's answer:
  it is the one the user has already seen and confirmed, and the callback may be a policy that never
  saw it. Anything left unanswered by both throws.

## 46. What it costs elsewhere

- `sync()` returning now means it applied — except under `dryRun`, which is the only reason `applied`
  survives as a field.
- The test helpers (`settle`, `stabilise`) wrap the throw, which is what a consumer will do too, so
  they stop being test scaffolding and start being the reference for what a caller writes.
- Every test that asserts on `pending` becomes a `rejects.toThrow` or a catch. That diff is again the
  honest measure of how much behaviour this moves.

## 47. Order

1. `ConflictError`, absorbing `PairingError`, with the payload shape of the contract.
2. Throw on unanswered conflicts; `dryRun` exempt.
3. `decide`, level 2 first — it is the same code path `decisions` already goes through.
4. `decide` for level 1, which is the part that turns a `throw` site into a question.
5. `path-collision` split out of `content` in `ConflictKind` (open decision 1), and the payload
   fields `readable` and `subtree` (open decision 3).

The harness clock (open decision 6) comes before all of it: `delete-edit` is the sharpest conflict in
the catalogue and no test has ever produced one.

---

# Walkthrough — 2026-08-13 06h20

**Summary**: steps 0–3 of §47 are in. 516 tests pass (511 before the change, 494 before this session
started), `lint`, `tsc --noEmit` and the build are clean. The clock fix uncovered a real bug (§49),
and one type had to be renamed to make room (§51). Level 1 stops in the new shape but cannot yet be
*answered* through `decide` — that is step 4, and it is the only part of §47 left.

**Status**: implemented, awaiting review.

## 48. The harness clock, and what it was hiding

`peer()` handed its nodes `tick()`; every test called `sync()` without `now`, so the pass used
`Date.now()`. Three years apart — entries stamped 2023, peer marks stamped 2026 — and
`pruneTombstones` keeps a tombstone only while `updated >= floor`. **Every tombstone was prunable the
moment it was written.**

So the suite had been exercising the pruned-tombstone path everywhere, and a deletion never reached
the merge as a deletion: the far side saw an absence and `solo()` reconstructed it as a propagation.
`delete-edit` — one of the four kinds, documented with a UI recommendation — could not be produced at
all. There is now a test that produces one, the first in the repo's history.

The fix is `sync`/`syncMesh`/`syncUntilStable` re-exported from the helpers with the shared clock, and
the reason is written where someone will hit it. Time in a test is now only what the test advances.

## 49. What it uncovered: every directory deletion was a conflict

With tombstones surviving, one test failed — and it was not the test's fault.

A directory has **no hash**, so `descends()` cannot prove that removing one is a propagation, and the
merge classified `roms` deleted on A against `roms` still live on B as `delete-edit`. Harmless while
conflicts only meant "a copy nobody asked for": no content, no copy, nobody noticed. Under the abort
rule it is fatal — **every folder anyone deletes would stop the mesh until a human said so**.

`needsDeciding` now also excludes a conflict where neither side has content: nothing can be lost by
taking the winner, and there are no two versions for a person to compare. Any file that *was* under
the directory is an entry of its own and conflicts on its own terms. Two tests pin the pair — the
directory case that must not stop, and the delete-versus-edit that must.

## 50. Throw or decide

`ConflictError` carries `conflicts: ConflictPayload[]`, one payload per unanswered conflict, in the
shape of [`docs/conflicts.yaml`](../docs/conflicts.yaml). `PairingError` is gone: a pairing refusal is
now an array of one with `level: 'pairing'`, which is what the contract said it was.

- **No way to answer** → throws. A `pending` in a returned result can be ignored by accident, and a
  pass that stopped then looks exactly like a pass that had nothing to do.
- **`decide` answered `abort`, or nothing** → returns, `applied: false`. Being asked and saying no is
  an answer.
- **`dryRun`** → never throws. It reports, and it stays additive: delete the flag and nothing else
  changes.

`decisions` moved to the same vocabulary — `{ id, action, side }` instead of `{ uuid, choice }` — so
the array and the callback are one language. The array wins when both answer the same conflict: it is
what a person has already seen and confirmed, while the callback may be a policy that never looked.

## 51. `ConflictReason` had to be freed

`types.ts` already had a `ConflictReason`: `'binary' | 'block' | 'delete-edit' | 'kind'`, the label a
*parked copy* carries in `vfs.json`. The catalogue's discriminator wanted the same name and means
something else — a case in the list of things the engine stops for.

The durable one is now **`CopyReason`**, which is what it always described: it appears nowhere except
on a conflict copy. Breaking, and the better name of the two.

## 52. What is left of §47

| | |
| --- | --- |
| 4. `decide` for level 1 | not started — the payload arrives, the answer (`adopt`, `reidentify`) does not |
| 5. `path-collision` split out of `content`, and `readable` / `subtree` | not started |

`adopt: { syncId }` still authorises a `foreign-mesh` the way it did, so nothing regressed; it is just
not yet the same door as everything else.

## 53. Commit message

```
one error, one vocabulary: every conflict now stops the pass and asks
```

---

# Walkthrough — 2026-08-13 07h10

**Summary**: §47 is finished. Steps 4 and 5 landed — the pairing question is answered through the same
callback as everything else, and `path-collision` is its own reason with the payload fields a
decision actually needs. 523 tests pass, `lint`, `tsc --noEmit` and the build clean. Two things came
out wrong and were corrected against the code rather than the plan (§55, §57).

**Status**: implemented, awaiting review.

## 54. Level 1 answers, through the same door

`pair()` no longer throws. It returns the refusal, `settlePairing()` puts it to `decisions` and then
to `decide`, and only an unanswered one becomes a `ConflictError`. `{ action: adopt }` and
`{ action: reidentify }` both work; `version-unreconcilable` still has only `abort`, because the
migration that `override` would need does not exist and an action in a contract is a promise.

**A `reidentify` lands during planning, before the merge**, and stays even if a file conflict stops
the pass a second later. That is deliberate: it repairs the two folders' relationship so that a merge
is possible at all — with one `peerId` on both sides, `peers[b.peerId]` is A's own mark and the log
offsets are already wrong. The identity was broken, the caller said which side fixes it, and retrying
the pass costs nothing.

## 55. `adopt` authorises; it does not choose

The contract said *"both take that side's syncId"*. The engine takes **the smaller of the two**, and
that is not a detail to paper over: it is what makes a mesh whose edges are authorised separately, by
different people on different days, still settle on one value. Naming a side proves you saw *this*
collision — the same reason `adopt: { syncId }` is specific rather than a boolean — and the outcome is
not the caller's to pick.

So `side` means "the side that stays as it is" for every action **except** `adopt`, where it is
proof rather than choice. Written down in both the type and the contract, because it is exactly the
kind of thing that gets re-derived wrongly a year from now.

## 56. `path-collision` exists now

`ConflictKind` gains it, and `resolvePathCollisions` reports same-kind collisions under it instead of
`content`. A consumer no longer has to compare `a.uuid` with `b.uuid` to find out which question it
is being asked.

## 57. The scenario that is *not* a path collision

Writing its test turned up the thing worth knowing: **two files created independently at the same
name are not a collision.** The path fallback in `pairEntries` pairs them as one file discovered
twice and reports `content` — which is right, and is what that fallback exists for.

A real collision is two entries *both sides already know* ending on one name: a rename from either
end, or a conflict copy landing on a name that is taken. The test uses the first, and the
distinction is now in the contract so the next person does not have to find it by experiment.

## 58. The payload carries what the decision needs

- **`readable`** per version — whether those bytes can be read from the peer holding them. Without
  it no UI can offer a diff, and every consumer would derive it from `held` and `mtime` its own way.
- **`subtree`** — entries under a directory, `0` for a file. *"This moves 412 files"* is the whole of
  a directory decision.
- **`uuid`** per side, which is what separates `content` from `path-collision` at a glance.
- **`everSynced`** and **`formatReadable`** per folder. The second is *not* the version `readable`:
  one is about a format this engine can parse, the other about bytes on disk. Two meanings, two
  names — the lesson of §51 applied before it became a bug this time.

## 59. What is left

Nothing of §47. Outside it: `keep-both` on a `delete-edit` is still the gap the contract marks with
`!` (a winning deletion parks no copy under `conflictCopies: 'edits'`), `location` is still never
asked, and the explorer still confirms through `approveMerge` instead of showing what stopped a pass.

## 60. Commit message

```
both levels answer to the same question now
```

---

# Extension — 2026-08-13 11h20

**Summary**: the `!` of §59 is real, and it is not alone. Every answer goes through one function
written for one shape — two versions of one file — and four reasons now arrive there. Three
consequences, two of them silent loss of a file, all three reproduced against `main` @ `7d5ef1e`
before anything was written. Planned here, not implemented.

## 61. What the probes came back with

Three throwaway runs, since a gap the contract *marks* is worth measuring before it is fixed:

| Answer | What it promises | What happens today |
| --- | --- | --- |
| `keep-both` on `delete-edit`, deletion winning | the deletion applies, the edit is parked beside it | **both folders end empty.** `applied: true`, `node.conflicts()` empty, no copy, no record. The edited version is gone and nothing says it existed |
| `keep-both` on `content` | the question is answered | the copy is parked and the file entry is left untouched — so **a third peer holding the loser raises the same conflict again**, and every new edge in the mesh re-asks it forever |
| `keep, side: a` on `path-collision` | the keeper keeps the name, the loser stays at the name it was moved to | A ends holding one file with the **loser's** bytes; B ends holding two. **A successful pass leaves the two folders in different states**, and one file is gone from A |

The third is the worst of the three and was not on anyone's list. It is one `expect` away from the
test that §57 wrote: that test asks what the payload *says*, and nobody had yet answered one.

## 62. One cause, and it is a shape mismatch

`applyDecisions` ([sync.ts:1384](../src/sync.ts#L1384)) assumes every report is a content dispute:
`report.uuid` is *the* file, `a` and `b` are two versions of it, and settling means minting a new
version. True for `content` and `delete-edit`. False for `path-collision` and `kind`, where `uuid` is
the **loser**, `a` is a **different file**, and the answer means *which one keeps the name* — nothing
is overwritten and there is no shared identity to mint anything for. Handed such a report, the
function writes the keeper's content over the loser's entry, which is exactly what the probe saw.

`keep-both` fails differently: it is not treated as an answer at all. It marks the conflict settled
and returns ([sync.ts:1419](../src/sync.ts#L1419)) on the reasoning that *"this is the outcome the
merge already reached"*. The merge reached what the **policy** said, and a policy is not an answer.

## 63. `conflictCopies` is a policy; an answer is a person

The rule that settles the first row of §61, and it is worth stating once for everything that comes
after: `conflictCopies` governs a pass **nobody is watching** — `'edits'` deliberately lets a winning
delete really delete ([merge.ts:67](../src/merge.ts#L67)). A decision is a person, present, naming
this file. When the two disagree, the answer wins, **including against `conflictCopies: false`**: a
caller who sets `false` and then answers `keep-both` has contradicted themselves, and the more
specific statement is the one made about this file.

## 64. The fix, in three parts

**(a) `keep-both` parks the copy it promised.** On a content-dimension report with no `report.copy`,
mint one now — the same `conflictCopy` the merge would have made, so the uuid stays the deterministic
`conflict:<uuid>:<hash>` and two peers answering the same dispute independently still produce one
copy rather than two. `conflictName` and `heldAt` still apply; `conflictCopies` does not (§63).

**(b) `keep-both` mints the winner, like every other answer.** Winner's content, `updated` above
both, `prev` the winner's hash, `prev2` the loser's — the §4 rule, from which `keep-both` is today
the only exemption, and the second row of §61 is the bill for that exemption. The parked copy stays a
durable pending conflict and `node.resolve()` is still what clears it; what stops is the *mesh*
re-asking a question a person already answered.

**(c) A path answer renames; it never overwrites.** `path-collision` and `kind` leave the
content-minting path altogether. Two halves, and both are needed:

- **`keep` naming the side that already keeps the path is a no-op** — and today it is the corruption
  of §61's third row. Removing the report from that code path is the whole fix.
- **`keep` naming the side that was moved aside is a swap**, and the honest way to perform it is not
  to unpick the rename afterwards but to tell the merge who the keeper is:
  `resolvePathCollisions` picks with `pickNewer` ([merge.ts:411](../src/merge.ts#L411)), so it takes
  a preference `path -> uuid` and the answer feeds a **second `mergeEntries`** — the same re-merge the
  archive read already performs ([sync.ts:578](../src/sync.ts#L578)). The aside rename, the subtree
  that travels with a directory, and the fixed point all stay in the one function that gets them
  right.

  This is why `applyDecisions` splits in two: **collect** every answer (from `decisions`, then
  `decide`), then **apply** them. A path preference triggers one re-merge, and the collected answers
  are applied to the new result without asking anybody a second time.

Not doing the second half is not an option once the first lands: `side: 'b'` would then quietly give
you `side: 'a'`, which is the same lie in a new place.

## 65. What stays as it is

- **`keep-both` is not offered on `path-collision` or `kind`**, and the contract never offered it —
  nothing is at risk there, both entries survive, "both" is already what happens. An answer a reason
  does not admit **leaves the conflict unanswered**, so the pass stops and reports it, which is the
  one behaviour that cannot lose anything and needs no new error.
- `location` still is not asked, and the explorer still confirms through `approveMerge` (§59).

## 66. Tests

- The first row of §61, as a test: `delete-edit`, deletion winning, `keep-both` → the deletion is on
  both sides **and** the edit is beside it under a conflict name, with `node.conflicts()` reporting
  one. The test that currently pins the opposite does not exist, which is how this survived.
- Same answer under `conflictCopies: false` → the copy is still parked (§63).
- The second row: `keep-both`, then a third peer holding the loser syncs — and does **not** stop. The
  fixture already exists at [decisions.test.ts:182](../test/decisions.test.ts#L182); this is that
  test with a different answer.
- The existing `'both'` test keeps its assertion that a pending conflict remains — minting the winner
  must not clear the parked copy, and that line is what proves it.
- The third row, both ways: `keep, side: a` on a path collision leaves the keeper's bytes at the
  contested name, the loser's at the aside name, and **`files(a)` equal to `files(b)`**; `keep,
  side: b` swaps which is which. The equality is the assertion that would have caught this.
- A `kind` collision answered toward the directory, so the subtree moves with it.

## 67. What it costs

`applyDecisions` becomes `collectAnswers` + `applyAnswers`, which is the shape it should have had
once a second dimension arrived. `mergeEntries` takes one more option. `docs/conflicts.yaml` loses
the `!` on `delete-edit`, and `known_gap`/`durable_reason` are untouched — `'block'` is a different
gap and stays open.

## 68. Commit message

```
an answer that keeps both now keeps both, and one that renames stops overwriting
```

---

# Walkthrough — 2026-08-13 12h06

**Summary**: §64 is in, all three parts. 531 tests pass (523 before), `lint`, `tsc --noEmit` and the
build are clean. The 8 new tests were run against the unfixed source with the fixes stashed: **7 of
them fail there**, which is the only evidence that a test pins anything. The eighth — a directory
losing a name and taking its subtree — passes either way and is kept as a regression guard on the
half of §70 that already worked. Two things came out differently from the plan and were decided
against the code: a fourth bug the `kind` test uncovered (§70), and one answer the plan wanted to
refuse and should not (§71).

**Status**: implemented, awaiting review.

## 69. What landed

`applyDecisions` is now `collectAnswers` + `applyAnswers`, split because the two dimensions settle in
a different order: an answer about a **name** changes the tree the merge produces, so it is asked
first and fed back into a second `mergeEntries` through `MergeOptions.keepers`; an answer about
**contents** is applied to the tree that comes out. Each conflict is still put to somebody exactly
once — the answer map spans both rounds.

- **`keep-both` parks the copy it promised**, minted by `copyForAnswer` with the merge's own
  deterministic uuid, so two peers answering the same dispute produce one copy and not two.
- **`keep-both` mints the winner with both parents**, like every other answer. It was the only one
  exempt, and §61's second row was the bill.
- **A path answer never enters the content path.** `conflictKey` files a content dispute under its
  uuid and a name collision under `path:<name>` — because answering a collision *swaps which entry
  yields*, so the report that comes back from the second merge names the other uuid and it is the
  same question, already answered. Keying by uuid would have asked it again forever.

`keepersFrom` reads `side` only from the **first** merge's reports, for the same reason: read it off
the second and the answer flips back on every pass.

## 70. The fourth bug, which the `kind` test walked into

Writing "answer a directory-versus-file collision" produced a tree with `shared` holding a file
**and** a directory at once, and the directory's own children hanging under the file's aside name.
`resolvePathCollisions` moves the loser's subtree by path prefix
([merge.ts:441](../src/merge.ts#L441)) — and when the loser is the *file*, everything under the
contested name belongs to the **keeper**, so the keeper's children were dragged out from under it.

It is not the decision's fault and it predates this session: `pickNewer` picks the directory whenever
it is the newer entry, and the same corruption follows with nobody deciding anything. The test for
that is written without any `decisions` at all, which is the honest way to file it.

The fix is that a file never claims a subtree, because it has not got one. Two entries on one path is
the single thing the function's own comment says the tree may not contain — the second one written
wins on disk and the other is silently gone.

## 71. `keep-both` on a name collision is an answer, and §65 was wrong

The plan said `keep-both` there should be left outstanding, since the contract does not offer it.
Implementing that broke two convergence seeds, and the failure was the argument: `stabilise` answers
every conflict with `keep-both`, which is the shape of a real policy — *"never lose anything"* — and
under the rule it could never get past a name collision. A mesh deadlocked over a question whose
answer is already true.

**Both entries do survive a path collision.** A caller saying "keep both" is describing the outcome
correctly, so it is accepted as a confirmation. `replace` stays refused there and always will be:
bytes cannot answer which of two files keeps a name.

## 72. What the contract says now

`docs/conflicts.yaml` loses the `!` on `delete-edit`, gains `keep-both` on `path-collision` and
`kind` with the reason it is a confirmation, and gains one rule above the catalogue: a content answer
**mints**, a name answer **renames**, and `replace` exists only in the first dimension.
`docs/conflicts.md` says where `conflictCopies` stops governing — the policy is for a pass nobody is
watching, and an answer outranks it for the one file it names, `false` included.

## 73. What is still open

Unchanged from §59: `location` is never asked, the explorer still confirms through `approveMerge`
instead of showing what stopped a pass, and `CopyReason 'block'` is declared and never written.
Minor rot found in passing and left alone: `docs/api.md` still documents `approveMerge`/`approved`,
and [sync.ts:981](../src/sync.ts#L981) refers to a `syncDryRun()` that no longer exists.

## 74. Commit message

```
an answer that keeps both now keeps both, and one that renames stops overwriting
```

---

# Extension — 2026-08-13 12h20

**Summary**: the explorer answers conflicts. Three sessions have built a way for a caller to be asked
and to answer, and the only consumer in the repo still cannot do either — it prints the exception.
Planned here, not implemented.

## 75. Where the explorer is

Half of it exists, and it is the second half. `resolveConflict`
([model.ts:1662](../explorer/src/model.ts#L1662)) already settles a **parked copy** after the fact
through `node.resolve()`, refuses when the bytes stayed on another peer, and logs what it kept. That
is the 4am path, and it works.

The first half is missing entirely. A pass that stops for a conflict throws `ConflictError`, and
[model.ts:1811](../explorer/src/model.ts#L1811) catches it with `this.log(String(error), 'warn')` —
the same line that catches a disk error. So the user gets `ConflictError: gamelist.xml needs a
decision` in the activity log, nothing is written, and **every later sync of that edge stops in the
same place** with no way to get past it. The library grew a door and the app has no handle on it.

## 76. The three-second timer decides the shape

`setAutoSync` fires `syncTarget()` on an interval ([model.ts:1862](../explorer/src/model.ts#L1862)),
and `syncTarget()` is *also* what the plain Sync button calls. A modal that opens every three seconds
because two devices disagree about one file is not a feature.

So the split the documentation already names — *"the abort path is for an edge with a person on it;
the park path is for an edge without one"* — stops being a sentence and becomes two code paths:

| | Asks? |
| --- | --- |
| Sync button, sync-with-confirm | **yes** — a person started it and is looking at it |
| the auto-sync timer | **no** — it logs what stopped the edge, once per conflict, and moves on |

The explorer is the first consumer to need that distinction, which is the evidence it was worth
having. The timer's log line has to say what a person can do about it, or it is the same shrug in
nicer words.

## 77. Shape

`ExplorerDialog.kind` gains `'decide'`, carrying the payloads and the answers picked so far.
`askDecision(conflicts)` resolves to `SyncDecision[]` or `null` for a cancel — and a cancel writes
nothing, which is already true and costs no code.

```ts
try {
  await sync(a.node, b.node);
} catch (error) {
  if (!(error instanceof ConflictError)) throw error;
  const decisions = await this.askDecision(error.conflicts);
  if (decisions) await sync(a.node, b.node, { decisions });
}
```

Retry rather than one attempt: answering can surface a conflict that was not there before, and the
folders may have moved while the dialog was open. This is `settle()`
([helpers.ts:173](../test/helpers.ts#L173)) with a loop instead of a single second chance, which is
the reference a consumer was always going to copy.

**Not `decide`.** The callback is asked once per conflict *during* the pass, so five disputed files
would open five modals in a row while the pass is held open. `ConflictError` plus `decisions` shows
all of them at once and applies them in one go, which is both the better dialog and the shape those
two were built for. `decide` stays right for a headless policy, which is not what a person at a
screen is.

The two are **not** the same operation, and the difference is worth knowing before choosing: `decide`
answers inside one pass, while the retry re-plans from scratch — two scans per round, and on Drive
the scan is most of what a sync costs. What the retry buys for that price is a dialog that sees every
conflict before answering any of them, and the staleness fields (`a`, `b`) that let an answer given
minutes ago be refused rather than applied to a version nobody saw.

## 77b. The retry does not terminate today, and that is a bug in the engine

Written as a loop, the flow above spins forever on an answer the reason does not admit — `replace` on
a `path-collision`, say. Confirmed against `main` @ `57fca6c`: the answer is collected, `applyAnswers`
declines to apply it, the conflict stays outstanding, the pass throws the *same* conflict, and the
loop asks again. Three rounds, three identical refusals, no progress and no error.

That is §65 and §71 being wrong in the same place. Both argued that leaving an inadmissible answer
outstanding "cannot lose anything and needs no new error". It loses nothing and it hangs the one
consumer shape the whole design points at, which is worse.

**An answer a reason does not admit is a caller mistake, not a conflict**, so it belongs where the
other caller mistakes are: thrown, immediately, naming the reason, the action, and what that reason
*does* admit. `legalAnswers` (§80) is what makes that message worth reading. It ships with this, and
it goes in first — a plan for a UI built on a loop that cannot terminate is not a plan.

## 78. What a row shows, and what it offers

Every field this needs is already in the payload — §58 put `readable`, `subtree` and the per-side
`uuid` there for exactly this, and this is the first thing to read them:

| `reason` | What the row shows | What it offers |
| --- | --- | --- |
| `content` | both sides' peer, size and time, `textReason` when a merge was tried and refused | keep this one · keep that one · keep both |
| `delete-edit` | which side is the deletion, and when | confirm the deletion · resurrect the edit · keep both |
| `path-collision` | the contested name, and the name the loser is at | which one keeps the name |
| `kind` | the same, plus `subtree` — *"this moves 412 files"* | which one keeps the name |
| `foreign-mesh`, `peer-collision` | both folders' entry counts and `everSynced` | adopt · reidentify · cancel |
| `version-unreconcilable` | `engine` against each folder's `version` | cancel, and say why there is nothing else |

The pairing cases are in from the start rather than left for later: opening a copied folder is
something a person does in this app in about four clicks, and `peer-collision` is what comes back.

## 79. `readable: false` is a warning, not a disabled button

Choosing a version whose bytes live on a third peer is one of the `not_decisions` — the engine
throws rather than pretend. `resolveConflict` already has that rule for `held`
([model.ts:1665](../explorer/src/model.ts#L1665)) and says *"sync with it first"*. The decide dialog
says the same thing in the same words, on the side that cannot be served. One rule, phrased once.

## 80. The contract should say which answers are legal, in code

The table in §78 is the contract restated in TypeScript, and the explorer is about to be the second
place it is written down — the first being `docs/conflicts.yaml`, which no consumer can read. Every
consumer after this one would re-derive it, wrongly and quietly, exactly as they would have re-derived
`readable`.

So the library exports it: `legalAnswers(reason)` returning the actions the contract admits for that
reason. Small, pure, tested against the catalogue in the library's own suite, and it is what the
explorer switches on. It also gives the `!`-style gaps somewhere to be true rather than documented.

## 81. `approveMerge` goes

Decided by the consumer, and §7 said so first. It takes `SyncResult.approved` with it, the
`approveMerge` and `approved` rows in `docs/api.md`, and the explorer's confirm flow, which becomes
what it always described: `sync(a, b, { dryRun: true })` to show the plan, then `sync(a, b)` to
perform it.

**What that costs, stated rather than discovered**: those are two passes, and the folders are free to
move between them — the plan the user approved is not literally the plan that runs. The hook's one
merit was that it previewed and applied inside a single pass. In this app the window is a click wide
and the next pass heals anything that lands in it; on a slower backend it is a real gap, and the
honest place to close it is a draft object (§3), which §17 deleted for good reasons. So: not
reintroduced, and written down here so the next person does not rediscover it as a surprise.

## 82. Tests

There is no explorer suite and this does not add one — the app has no test infrastructure, and
standing one up is a session of its own, not a rider on this one.

What that costs is stated rather than hidden: the wiring and the presentation are verified by
running it (`npm run explorer`, two roots, one disputed file). What is *not* left to that is the part
worth pinning — `legalAnswers` (§80) is library code with library tests, and the engine behaviour
underneath it already has 531.

## 83. Open

Whether the timer, on the edge it cannot ask about, should pass `conflictCopies` and let the mesh
keep converging — the "park" half of §15. It is the honest behaviour for a background sync and it is
a behaviour change to the app's defaults, so it is called out rather than slipped in.

## 84. Commit message

```
the explorer can answer what it was only able to report
```

---

# Walkthrough — 2026-08-13 15h51

**Summary**: §75–§84 are in. 536 tests pass (533 before, 531 at the start of the day), `lint`,
`tsc --noEmit`, the library build and the explorer build are all clean. Two departures from the plan,
both because the code said otherwise: §82 said no explorer suite and there is one (§88), and the
retry loop turned up a deadlock in the dialog itself (§89).

**Status**: implemented, awaiting review.

## 85. The contract is code now

`legalAnswers(reason)` returns the actions the catalogue admits, and **`AnswerError`** is thrown at
the call that supplied anything else, carrying `reason`, `action` and `allowed`. Validation happens
where an answer is collected — once for entry conflicts, once for the pairing refusal — so the two
levels cannot drift apart.

That deletes the silence §77b was about. It also deletes two branches in `applyAnswers` that existed
only to skip an answer that could not arrive: an impossible branch is a claim about the code that
stops being true without anyone noticing.

## 86. `approveMerge` is gone

With it goes `SyncResult.approved` and its row in `docs/api.md`. The explorer's confirm button is now
`dryRun` to show the plan and `sync()` to run it, which is what the flag was always for. The test
that pinned the veto is replaced by one that pins the replacement — look, then don't call the thing
that writes. `docs/api.md` says plainly what the hook bought and what the two calls cost instead, so
its removal reads as a decision rather than an omission.

## 87. The timer never asks

`syncTarget()` was both the Sync button and the auto-sync tick, so wiring a dialog into it would have
opened a modal every three seconds. The tick is `syncTargetQuietly()` now: it reports what stopped
the edge and returns. `reportStopped` keys on the set of conflicts, so a stuck edge writes **one**
line, not one per tick — with a 3s timer the difference between a log and a flood.

`syncAll` asks too, and had to: `ALL_ROOTS` is the default target, so the common button goes through
it. `syncUntilStable` already sets a stopped edge aside and carries on, so what comes back is every
conflict the chain could not settle, deduplicated by id and asked once.

## 88. There is an explorer test, against §82

The plan said standing up a suite was a session of its own. That was wrong, and the way to find out
was to write the throwaway script §82 proposed instead: the model is plain TypeScript over adapters,
`boot()` runs under Node with no DOM, and a subscriber standing in for a user is exactly what the
components are. Fifty lines, no infrastructure, three tests:

- a stopped pass reaches a person, the answer reaches `sync()`, and `keep-both` lands both versions
  on both roots with a resolvable copy each;
- cancelling writes nothing;
- three auto-sync ticks open **zero** dialogs, log one line, and leave the folder as it was.

The choices offered are asserted against `legalAnswers`, so the dialog cannot drift from the engine.

## 89. The deadlock the loop found

Answering *inside* the emit that announced the dialog hung forever. `askDecision` emitted first and
installed its resolver second, so a subscriber that answered synchronously called `closeDialog` while
`dialogResolve` was still `null` — the answer was given before there was anything to receive it, and
the dialog then waited for one that had already happened.

A person with a mouse never hits it. A test does, and so does any policy that answers without a
human. Resolver first, emit second — and the same ordering was in `askConfirm` and `askPrompt`, which
predate this session and are fixed with it. Confirmed the test catches it by reverting the fix: two
of the three time out.

## 90. What is left

`location` is still never asked, and `CopyReason 'block'` is still declared and never written.
[sync.ts:981](../src/sync.ts#L981) still names a `syncDryRun()` that does not exist — cosmetic, and
left alone. The `docs/api.md` `pending` row was wrong about its own type (`ConflictReport[]` where
the field is `ConflictPayload[]`) and is corrected.

## 91. Commit message

```
the explorer can answer what it was only able to report
```

---

# Extension — 2026-08-13 16h10

**Summary**: the last `known_gap` in the contract. `CopyReason 'block'` is declared, documented, given
a UI recommendation, and never written — so a copy parked after a failed text merge tells the app
the opposite of what happened.

## 92. Two vocabularies for "why", with two lifetimes

| | `textReason` | `CopyReason` |
| --- | --- | --- |
| Type | `Diff3Reason \| 'no-base' \| 'unreadable'` | `'binary' \| 'block' \| 'delete-edit' \| 'kind'` |
| Lives on | the conflict **report** of one pass | the parked **copy entry**, in `vfs.json` |
| Lifetime | the pass — it reaches `decide` and `ConflictError`, then it is gone | durable: it converges through the mesh and `node.conflicts()` reads it back months later |

They overlap in exactly one value, `block`, and that overlap is the bug.

## 93. The copy is labelled before anyone has read a byte

`conflictCopy` ([merge.ts:388](../src/merge.ts#L388)) derives the reason from the conflict's kind:

```ts
const reason: CopyReason = kind === 'delete-edit' ? 'delete-edit' : kind === 'kind' ? 'kind' : 'binary';
```

At that point the merge is pure — it has compared two entry lists and read no content. It cannot know
whether the file is text, whether a merge will be attempted, or how it would fail. So everything that
is not a delete or a kind collision is called `binary`.

The text merge runs afterwards, in `autoMergeText`. It reads both versions, runs `diff3`, and when
diff3 declines because both sides edited the same lines it records
`report.textReason = 'block'` ([sync.ts:1345](../src/sync.ts#L1345)) — **on the report, which is
about to be thrown away.** The copy that survives is never told.

## 94. What it costs, which is not cosmetic

`docs/conflicts.md` has a table of what a UI should offer per durable reason:

| `reason` | What a UI should offer |
| --- | --- |
| `binary` | keep mine / keep theirs |
| `block` | a merge view, manual editing |

So the app resolving a parked copy later — the 4am path, `node.resolve()`, the half of the explorer
that already works — is told there is nothing to merge, about a text file whose lines collided and
for which a three-way view is exactly the right answer. `binary` is false on all three counts: it was
text, a merge *was* attempted, and it failed for a reason the caller can act on.

## 95. The fix, and its one safety question

Stamp the copy where the reason is known and the copy is in hand. `report.copy` is the same object as
the entry in the merged tree, so writing it there is writing what gets saved.

Only `block` crosses over: `eol`, `size` and `no-base` have no durable value, and inventing them
would widen a type that travels to every peer's `vfs.json` — a bigger decision than this, and not one
anybody has asked for.

**Does a late change to `reason` threaten convergence?** No, and it is worth writing down why:
`DIGEST_FIELDS` ([vfs-file.ts:31](../src/vfs-file.ts#L31)) is `uuid, kind, path, hash, size, updated`.
`reason` is not in it. Two peers that disagreed about a copy's label would still agree on `state`, so
nothing can ping-pong — which also means a peer that reaches the same copy without attempting a merge
(`autoMerge: false`) writes `binary` and converges anyway.

## 96. Tests

- A text conflict whose lines collide, parked: `node.conflicts()[0].reason` is `'block'`, not
  `'binary'`. The suite has never asserted on a durable reason other than by accident.
- A binary conflict still parks `binary` — the label has to keep meaning something.

## 97. Commit message

```
a copy parked after a failed text merge stops calling itself binary
```

---

# Walkthrough — 2026-08-13 16h43

**Summary**: §95 is in — one line, where the reason is known and the copy is in hand. 537 tests pass,
`lint`, `tsc --noEmit` and the build clean. The interesting part is not the fix (§98).

## 98. It was not an oversight. A test held it in place

`conflicts.test.ts` had this, and had had it for as long as the durable reason has existed:

```ts
await settle(a.node, b.node);
const [pending] = await a.node.conflicts();
expect(pending?.reason).toBe('binary');
```

That is a text file, `gamelist.xml`, whose lines collided — the exact case `block` was invented for —
asserting the label that says *"there is nothing to merge here"*. The gap was not a value nobody got
round to writing; it was a value the suite actively required to be wrong.

Which is why it survived being declared in the type, described in `docs/conflicts.md` with a UI
recommendation, and named in the contract as a `known_gap`. Three documents said `block` existed; one
test said it did not, and the test is the one the code listens to.

The corrected assertion also now checks the far side, because a copy converges and both peers have to
say the same thing about it.

## 99. What the fix does not do

`eol` and `size` stay ephemeral. They are real reasons a merge did not happen, and `eol` in
particular is a standing condition of the folder rather than an accident of one pass — but giving
them durable values widens a type that travels to every peer's `vfs.json`, and no consumer has asked.
The asymmetry is written into both the contract and `docs/conflicts.md` so the next person meets it
as a decision rather than an omission.

## 100. The contract has no known gaps left

`durable_reason.known_gap` is gone, replaced by a note on why the label is assigned in two steps and
why `reason` is safe outside `DIGEST_FIELDS`. The Status block no longer says "nearly", and it points
at `legalAnswers`/`AnswerError` as the reason a catalogue that drifts from the implementation now
fails a test rather than a user.

What the contract still describes and does not decide is `location`, which is never asked. That is
deliberate, it is explained where it is listed, and it is the one thing left on the list from §59.

## 101. Commit message

```
a copy parked after a failed text merge stops calling itself binary
```
